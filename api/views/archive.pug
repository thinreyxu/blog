extends layout.pug

block page-header
  h1.logo.ir= `${title}`

block content
  //- each posts, year in postsByYear
  //-   h2= `${year}`
  //-   ul
  //-     each post in posts
  //-       li
  //-         a(href=`/p/${post._id}`)= `${post.title}`
  include post-title-list.pug
